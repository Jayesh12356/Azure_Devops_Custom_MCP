# Optional: only needed for shared service-account mode (MCP_AUTHENTICATION=envvar).
# For per-user PAT mode (request-pat), do NOT store a shared PAT here.
apiVersion: v1
kind: Secret
metadata:
  name: azure-devops-mcp-secrets
  namespace: azure-devops-mcp
type: Opaque
stringData: {}
