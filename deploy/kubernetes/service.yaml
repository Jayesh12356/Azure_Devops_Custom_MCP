apiVersion: v1
kind: Service
metadata:
  name: azure-devops-mcp
  namespace: azure-devops-mcp
  labels:
    app: azure-devops-mcp
spec:
  type: ClusterIP
  selector:
    app: azure-devops-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
